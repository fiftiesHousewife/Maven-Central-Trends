{
  "name": "maven-central-trends",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for Maven Central publishing trends dashboard",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
